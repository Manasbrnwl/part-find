name = "part-find-worker"
main = "src/index.ts"
compatibility_date = "2024-02-12"
compatibility_flags = ["nodejs_compat"]

[vars]
# Environment variables used in the worker
# DATABASE_URL and others should be set via `wrangler secret put` for production
# or in .dev.vars for local development

[observability]
enabled = true
